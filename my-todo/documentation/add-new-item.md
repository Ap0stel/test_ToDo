# TODO: Добавить функционал добавления и удаления задач

## Общая концепция

Нужно добавить два новых функционала:
1. **Добавление задач** - пользователь может создавать новые задачи и добавлять их в список активных или завершенных задач
2. **Удаление задач** - пользователь может удалять существующие задачи из любого списка

При добавлении задачи она должна появляться в соответствующем списке (активные или завершенные), а при удалении - полностью исчезать из интерфейса.

---

## Шаг 1: Понимание текущей архитектуры

### Структура компонентов:
```
App
  └── TasksFetch (управляет состоянием и загрузкой данных)
      ├── AddBtn (кнопка для добавления новой задачи) [нужно добавить]
      └── TaskList (отображает список задач)
          └── Task (отображает одну задачу с кнопкой удаления)
```

### Текущее состояние данных:

В компоненте `TasksFetch.tsx` находится:
- **`todos`** - массив всех задач (State)
- **`activeTasks`** - вычисляемое значение через `useMemo` (задачи с `completed: false`)
- **`completedTasks`** - вычисляемое значение через `useMemo` (задачи с `completed: true`)

### Типы данных:

В файле `src/types/index.ts` определены два интерфейса:
```typescript
export interface Task {
  id: number;
  title: string;
  description?: string;  // Необязательное поле
  completed: boolean;
}

export interface Todo {
  userId: number;
  id: number;
  title: string;
  completed: boolean;
}
```

**Важно:** `Task` содержит `description`, а `Todo` - нет. В нашем проекте используется `Todo` для задач из API.

---

## Шаг 2: Создание функции для добавления задачи

### Что такое иммутабельность?

В React **нельзя изменять state напрямую**! Нужно создавать новые копии массивов и объектов.

❌ **Неправильно:**
```typescript
todos.push(newTodo);  // Мутация - так делать нельзя!
```

✅ **Правильно:**
```typescript
setTodos([...todos, newTodo]);  // Создаем новый массив
```

### 2.1. Создайте функцию добавления в TasksFetch.tsx:

**Где:** После функции `updateTaskTitle`, примерно на строке 61.

```typescript
const addTask = useCallback(
  (title: string, completed: boolean = false) => {
    // Проверяем, что название не пустое
    if (title.trim() === '') {
      return;
    }

    // Создаем новую задачу
    const newTodo: Todo = {
      id: Date.now(), // Используем временную метку как уникальный ID
      userId: 1,      // Устанавливаем значение по умолчанию
      title: title.trim(),
      completed: completed,
    };

    // Добавляем новую задачу в начало массива
    setTodos([newTodo, ...todos]);
  },
  [todos]
);
```

**Объяснение построчно:**

1. `useCallback` - это хук React для мемоизации функций (предотвращает пересоздание функции на каждом рендере)
2. `title.trim()` - удаляет пробелы в начале и конце строки
3. `completed: boolean = false` - параметр по умолчанию (если не передан, будет `false`)
4. `Date.now()` - возвращает текущее время в миллисекундах (уникальное значение для ID)
5. `[newTodo, ...todos]` - spread оператор `...` разворачивает массив `todos`, и мы добавляем новый элемент в начало
6. `[todos]` - массив зависимостей для `useCallback` (функция пересоздается только при изменении `todos`)

**Почему добавляем в начало?**
- Новые задачи будут отображаться первыми в списке
- Пользователь сразу увидит добавленную задачу

**Альтернатива (добавление в конец):**
```typescript
setTodos([...todos, newTodo]);
```

---

## Шаг 3: Создание функции для удаления задачи

### Метод filter() в JavaScript

Метод `filter()` создает **новый массив** с элементами, которые прошли проверку:

```typescript
const numbers = [1, 2, 3, 4, 5];
const evenNumbers = numbers.filter(num => num % 2 === 0);
// Результат: [2, 4]
```

### 3.1. Создайте функцию удаления в TasksFetch.tsx:

**Где:** После функции `addTask`, примерно на строке 76.

```typescript
const deleteTask = useCallback(
  (taskId: number) => {
    // Создаем новый массив без задачи с указанным ID
    const updatedTodos = todos.filter((task) => task.id !== taskId);
    setTodos(updatedTodos);
  },
  [todos]
);
```

**Объяснение:**

1. `filter()` проходит по всем задачам
2. `task.id !== taskId` - оставляем только те задачи, у которых ID **не совпадает** с удаляемым
3. Результат - новый массив без удаленной задачи

**Упрощенная версия (в одну строку):**
```typescript
const deleteTask = useCallback(
  (taskId: number) => setTodos(todos.filter((task) => task.id !== taskId)),
  [todos]
);
```

---

## Шаг 4: Создание UI для добавления задачи

### Концепция: Модальное окно (Modal)

**Модальное окно** - это всплывающий блок, который появляется поверх основного содержимого.

### 4.1. Создайте компонент AddBtn (если его нет)

**Где:** Создайте файл `src/components/AddBtn/AddBtn.tsx`

```typescript
import classes from "./AddBtn.module.css";
import { useState } from "react";

interface AddBtnProps {
  onAddTask: (title: string, completed: boolean) => void;
}

export default function AddBtn({ onAddTask }: Readonly<AddBtnProps>) {
  const [isModalOpen, setIsModalOpen] = useState<boolean>(false);
  const [title, setTitle] = useState<string>("");
  const [completed, setCompleted] = useState<boolean>(false);

  const handleSubmit = () => {
    if (title.trim()) {
      onAddTask(title, completed);
      // Очищаем форму после добавления
      setTitle("");
      setCompleted(false);
      setIsModalOpen(false);
    }
  };

  const handleClose = () => {
    setTitle("");
    setCompleted(false);
    setIsModalOpen(false);
  };

  return (
    <>
      <button
        className={classes["add-task-btn"]}
        onClick={() => setIsModalOpen(true)}
      >
        <h3>+ Добавить задачу</h3>
      </button>

      {isModalOpen && (
        <div className={classes.modal} onClick={handleClose}>
          <div
            className={classes.modalContent}
            onClick={(e) => e.stopPropagation()}
          >
            <h2>Добавить новую задачу</h2>

            <input
              type="text"
              placeholder="Название задачи"
              value={title}
              onChange={(e) => setTitle(e.target.value)}
              onKeyDown={(e) => e.key === 'Enter' && handleSubmit()}
              className={classes.input}
              autoFocus
            />

            <label className={classes.checkbox}>
              <input
                type="checkbox"
                checked={completed}
                onChange={(e) => setCompleted(e.target.checked)}
              />
              <span>Сразу отметить как завершенную</span>
            </label>

            <div className={classes.buttons}>
              <button onClick={handleSubmit} className={classes.saveBtn}>
                Добавить
              </button>
              <button onClick={handleClose} className={classes.cancelBtn}>
                Отмена
              </button>
            </div>
          </div>
        </div>
      )}
    </>
  );
}
```

**Объяснение ключевых моментов:**

1. **`useState<boolean>(false)`** - состояние для контроля открытия/закрытия модального окна
2. **`isModalOpen && (...)`** - условный рендеринг (модалка показывается только если `isModalOpen === true`)
3. **`onClick={handleClose}`** - клик на затемненный фон закрывает модалку
4. **`onClick={(e) => e.stopPropagation()}`** - предотвращает закрытие при клике на содержимое модалки
   - `stopPropagation()` останавливает "всплытие" события к родительскому элементу
5. **`onKeyDown={(e) => e.key === 'Enter' && handleSubmit()}`** - добавление задачи по нажатию Enter
6. **`autoFocus`** - автоматически фокусируется на поле ввода при открытии модалки
7. **`Readonly<AddBtnProps>`** - TypeScript утилита, делающая все свойства только для чтения

### 4.2. Создайте CSS для AddBtn

**Где:** Создайте файл `src/components/AddBtn/AddBtn.module.css`

```css
.add-task-btn {
  width: 100%;
  padding: 15px;
  margin-bottom: 20px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border: none;
  border-radius: 8px;
  cursor: pointer;
  transition: transform 0.2s, box-shadow 0.2s;
}

.add-task-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
}

.add-task-btn h3 {
  margin: 0;
  font-size: 16px;
  font-weight: 600;
}

.modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 1000;
}

.modalContent {
  background: white;
  padding: 30px;
  border-radius: 12px;
  min-width: 400px;
  max-width: 500px;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
}

.modalContent h2 {
  margin-top: 0;
  margin-bottom: 20px;
  color: #333;
}

.input {
  width: 100%;
  padding: 12px;
  margin-bottom: 15px;
  border: 2px solid #e0e0e0;
  border-radius: 6px;
  font-size: 14px;
  transition: border-color 0.2s;
}

.input:focus {
  outline: none;
  border-color: #667eea;
}

.checkbox {
  display: flex;
  align-items: center;
  margin-bottom: 20px;
  cursor: pointer;
}

.checkbox input {
  margin-right: 10px;
  cursor: pointer;
}

.checkbox span {
  font-size: 14px;
  color: #555;
}

.buttons {
  display: flex;
  gap: 10px;
  justify-content: flex-end;
}

.buttons button {
  padding: 10px 20px;
  border: none;
  border-radius: 6px;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s;
}

.saveBtn {
  background: #667eea;
  color: white;
}

.saveBtn:hover {
  background: #5568d3;
}

.cancelBtn {
  background: #e0e0e0;
  color: #333;
}

.cancelBtn:hover {
  background: #d0d0d0;
}
```

**Объяснение CSS:**

1. **`position: fixed`** - элемент фиксируется относительно окна браузера
2. **`z-index: 1000`** - модальное окно отображается поверх всех элементов
3. **`rgba(0, 0, 0, 0.5)`** - полупрозрачный черный фон (50% прозрачности)
4. **`box-shadow`** - тень для объемности
5. **`transition`** - плавные анимации

---

## Шаг 5: Добавление кнопки удаления в Task

### 5.1. Обновите интерфейс TaskProps в Task.tsx:

**Где:** В строке 5-9

**Было:**
```typescript
interface TaskProps {
  task: Todo;
  onToggle: (id: number) => void;
  onUpdateTitle: (taskId: number, newTitle: string) => void;
}
```

**Стало:**
```typescript
interface TaskProps {
  task: Todo;
  onToggle: (id: number) => void;
  onUpdateTitle: (taskId: number, newTitle: string) => void;
  onDelete: (id: number) => void;  // Добавили функцию удаления
}
```

### 5.2. Получите функцию в параметрах Task:

**Где:** В строке 11

**Было:**
```typescript
function Task({ task, onToggle, onUpdateTitle }: Readonly<TaskProps>) {
```

**Стало:**
```typescript
function Task({ task, onToggle, onUpdateTitle, onDelete }: Readonly<TaskProps>) {
```

### 5.3. Добавьте кнопку удаления в разметку:

**Где:** В строке 69, внутри `<div className={classes["task-actions"]}>`

**Было:**
```typescript
<div className={classes["task-actions"]}></div>
```

**Стало:**
```typescript
<div className={classes["task-actions"]}>
  <button
    className={classes["delete-btn"]}
    onClick={() => onDelete(task.id)}
    aria-label="Удалить задачу"
  >
    ✕
  </button>
</div>
```

**Объяснение:**
- `aria-label` - описание для скринридеров (доступность для людей с ограниченными возможностями)
- `✕` - символ крестика (можно заменить на иконку)

### 5.4. Добавьте стили для кнопки удаления:

**Где:** В файле `src/components/Task/Task.module.css`

```css
.delete-btn {
  background: transparent;
  border: none;
  color: #999;
  font-size: 20px;
  cursor: pointer;
  padding: 5px 10px;
  border-radius: 4px;
  transition: all 0.2s;
}

.delete-btn:hover {
  background: #ffebee;
  color: #f44336;
  transform: scale(1.1);
}

.delete-btn:active {
  transform: scale(0.95);
}
```

**Объяснение стилей:**
- **`transform: scale(1.1)`** - увеличивает кнопку на 10% при наведении
- **`transform: scale(0.95)`** - уменьшает при нажатии (эффект нажатия)
- **`transition: all 0.2s`** - плавная анимация всех изменений за 0.2 секунды

---

## Шаг 6: Передача функций через Props Drilling

### Концепция: Props Drilling

Данные передаются от родителя к детям через **props**. Чтобы функции из `TasksFetch` попали в `Task`, нужно пройти через `TaskList`.

```
TasksFetch (создаем функции: addTask, deleteTask)
    ↓ props
TaskList (пробрасываем функции дальше)
    ↓ props
Task (используем функцию deleteTask)
```

### 6.1. Передайте функцию addTask в AddBtn

**Где:** В файле `TasksFetch.tsx`, после строки 66 (в начале JSX)

```typescript
return (
  <>
    <AddBtn onAddTask={addTask} />

    <TaskList
      title="Активные"
      tasks={activeTasks}
      onToggle={toggleTask}
      onUpdateTitle={updateTaskTitle}
      onDelete={deleteTask}
    />
    <TaskList
      title="Завершенные"
      tasks={completedTasks}
      onToggle={toggleTask}
      onUpdateTitle={updateTaskTitle}
      onDelete={deleteTask}
    />
  </>
);
```

**Важно:** Не забудьте импортировать компонент в начале файла!

**Где:** В строке 3

```typescript
import AddBtn from "../AddBtn/AddBtn";
```

### 6.2. Обновите интерфейс TaskListProps

**Где:** В файле `TaskList.tsx`, строки 5-10

**Было:**
```typescript
interface TaskListProps {
  tasks: Todo[];
  title: string;
  onToggle: (id: number) => void;
  onUpdateTitle: (taskId: number, newTitle: string) => void;
}
```

**Стало:**
```typescript
interface TaskListProps {
  tasks: Todo[];
  title: string;
  onToggle: (id: number) => void;
  onUpdateTitle: (taskId: number, newTitle: string) => void;
  onDelete: (id: number) => void;  // Добавили
}
```

### 6.3. Получите onDelete в параметрах TaskList

**Где:** В строке 12-17

**Было:**
```typescript
export default function TaskList({
  tasks,
  title,
  onToggle,
  onUpdateTitle,
}: Readonly<TaskListProps>) {
```

**Стало:**
```typescript
export default function TaskList({
  tasks,
  title,
  onToggle,
  onUpdateTitle,
  onDelete,
}: Readonly<TaskListProps>) {
```

### 6.4. Передайте onDelete в Task

**Где:** В строке 25

**Было:**
```typescript
return <Task task={task} key={task.id} onToggle={onToggle} onUpdateTitle={onUpdateTitle}/>;
```

**Стало:**
```typescript
return (
  <Task
    task={task}
    key={task.id}
    onToggle={onToggle}
    onUpdateTitle={onUpdateTitle}
    onDelete={onDelete}
  />
);
```

---

## Шаг 7: Улучшение UX - подтверждение удаления

### Зачем нужно подтверждение?

Удаление - необратимая операция. Легко случайно нажать кнопку удаления, поэтому лучше попросить подтверждение.

### 7.1. Добавьте подтверждение в функцию deleteTask

**Где:** В файле `TasksFetch.tsx`, обновите функцию `deleteTask`

**Было:**
```typescript
const deleteTask = useCallback(
  (taskId: number) => {
    const updatedTodos = todos.filter((task) => task.id !== taskId);
    setTodos(updatedTodos);
  },
  [todos]
);
```

**Стало:**
```typescript
const deleteTask = useCallback(
  (taskId: number) => {
    // Находим задачу для отображения в диалоге
    const taskToDelete = todos.find((task) => task.id === taskId);

    if (!taskToDelete) return;

    // Показываем диалог подтверждения
    const confirmed = window.confirm(
      `Вы уверены, что хотите удалить задачу "${taskToDelete.title}"?`
    );

    if (confirmed) {
      const updatedTodos = todos.filter((task) => task.id !== taskId);
      setTodos(updatedTodos);
    }
  },
  [todos]
);
```

**Объяснение:**

1. **`find()`** - ищет первый элемент, который соответствует условию
2. **`window.confirm()`** - встроенный браузерный диалог с кнопками OK/Отмена
3. **`if (confirmed)`** - удаляем только если пользователь нажал OK

**Альтернатива:** Можно создать красивое кастомное модальное окно вместо `window.confirm()`.

---

## Шаг 8: Тестирование

### Проверьте функционал:

1. **Запустите dev сервер:**
   ```bash
   npm run dev
   ```

2. **Откройте приложение в браузере** (обычно http://localhost:5173)

3. **Протестируйте добавление задачи:**
   - [ ] Кликните на кнопку "Добавить задачу"
   - [ ] Модальное окно открывается
   - [ ] Введите название задачи
   - [ ] **Без чекбокса:** задача добавляется в "Активные"
   - [ ] **С чекбоксом:** задача добавляется в "Завершенные"
   - [ ] Модальное окно закрывается после добавления
   - [ ] Клик на затемненный фон закрывает модальное окно
   - [ ] Клик на "Отмена" закрывает модальное окно
   - [ ] Нажатие Enter добавляет задачу
   - [ ] Пустое название не добавляется

4. **Протестируйте удаление задачи:**
   - [ ] Кнопка удаления видна на каждой задаче
   - [ ] Клик показывает диалог подтверждения
   - [ ] Нажатие "OK" удаляет задачу
   - [ ] Нажатие "Отмена" не удаляет задачу
   - [ ] Задача исчезает из списка

5. **Проверьте взаимодействие функций:**
   - [ ] Добавьте активную задачу → переключите в завершенную → удалите
   - [ ] Добавьте завершенную задачу → она сразу в правильном списке
   - [ ] Редактирование названия работает для новых задач
   - [ ] Счетчики задач обновляются корректно

6. **Запустите линтер:**
   ```bash
   npm run lint
   ```
   Не должно быть ошибок и предупреждений

7. **Проверьте типы TypeScript:**
   ```bash
   npx tsc --noEmit
   ```
   Не должно быть ошибок типизации

---

## Дополнительные концепции React для понимания

### 1. useCallback - мемоизация функций

**Зачем нужен useCallback?**

Без `useCallback` функция пересоздается на каждом рендере:

```typescript
// ❌ Плохо - функция пересоздается каждый раз
const handleClick = () => {
  console.log("clicked");
};

// ✅ Хорошо - функция сохраняется между рендерами
const handleClick = useCallback(() => {
  console.log("clicked");
}, []);
```

**Когда использовать:**
- Когда функция передается как prop в дочерние компоненты
- Когда функция является зависимостью для `useEffect` или `useMemo`

### 2. Контролируемые компоненты (Controlled Components)

В нашем `AddBtn` инпуты являются контролируемыми:

```typescript
const [title, setTitle] = useState<string>("");

<input
  value={title}                           // React управляет значением
  onChange={(e) => setTitle(e.target.value)}  // Обновляем state при изменении
/>
```

**Почему это важно?**
- React является единственным источником правды (Single Source of Truth)
- Мы можем легко валидировать, форматировать и управлять данными
- Легко очищать форму программно

### 3. Условный рендеринг

Есть несколько способов условного рендеринга в React:

```typescript
// 1. Логическое И (&&)
{isModalOpen && <Modal />}

// 2. Тернарный оператор
{isLoading ? <Spinner /> : <Content />}

// 3. if-else в функции
function render() {
  if (isLoading) return <Spinner />;
  if (error) return <Error />;
  return <Content />;
}
```

### 4. Event.stopPropagation()

**Всплытие событий (Event Bubbling):**

```
<div onClick={handleOuterClick}>        ← 3. Затем всплывает сюда
  <div onClick={handleInnerClick}>      ← 2. Потом всплывает сюда
    <button onClick={handleClick}>      ← 1. Событие начинается здесь
      Click
    </button>
  </div>
</div>
```

**stopPropagation() останавливает всплытие:**

```typescript
<div className={classes.modal} onClick={handleClose}>
  <div onClick={(e) => e.stopPropagation()}>
    {/* Клик здесь НЕ вызовет handleClose */}
  </div>
</div>
```

### 5. Генерация уникальных ID

В нашем проекте используется `Date.now()`:

```typescript
id: Date.now()  // Пример: 1703251200000
```

**Альтернативные способы:**

```typescript
// 1. UUID (требует библиотеку)
import { v4 as uuidv4 } from 'uuid';
id: uuidv4()  // Пример: "110ec58a-a0f2-4ac4-8393-c866d813b8d1"

// 2. Простой счетчик
let nextId = 1;
id: nextId++

// 3. Случайное число (может быть коллизия!)
id: Math.random()
```

**Для production приложений рекомендуется UUID или ID от сервера.**

### 6. TypeScript - опциональные параметры

```typescript
function addTask(title: string, completed: boolean = false) {
  // completed по умолчанию false, если не передан
}

// Вызовы:
addTask("Задача 1");              // completed = false
addTask("Задача 2", true);        // completed = true
```

### 7. CSS Modules

Файлы `.module.css` создают локальные стили:

```typescript
import classes from "./Task.module.css";

<div className={classes.task}>  // Превратится в уникальное имя класса
```

**Преимущества:**
- Нет конфликтов имен классов между компонентами
- Автоматическая генерация уникальных имен
- Лучшая изоляция стилей

---

## Возможные ошибки и их решения

### Ошибка 1: Задача добавляется не в тот список

**Причина:** Неправильное значение параметра `completed`

**Решение:** Убедитесь, что:
```typescript
// Для активных задач
onAddTask(title, false);

// Для завершенных задач
onAddTask(title, true);
```

### Ошибка 2: TypeScript ругается на `onDelete` в Task

```
Property 'onDelete' does not exist on type 'TaskProps'
```

**Причина:** Не обновлен интерфейс

**Решение:** Добавьте в `TaskProps`:
```typescript
interface TaskProps {
  task: Todo;
  onToggle: (id: number) => void;
  onUpdateTitle: (taskId: number, newTitle: string) => void;
  onDelete: (id: number) => void;  // Не забыли добавить?
}
```

### Ошибка 3: Модальное окно не закрывается при клике на фон

**Причина:** `stopPropagation()` применен неправильно

**Решение:**
```typescript
<div className={classes.modal} onClick={handleClose}>
  <div onClick={(e) => e.stopPropagation()}>
    {/* Содержимое модалки */}
  </div>
</div>
```

### Ошибка 4: После добавления задачи форма не очищается

**Причина:** Забыли очистить state

**Решение:**
```typescript
const handleSubmit = () => {
  if (title.trim()) {
    onAddTask(title, completed);
    // Очистка
    setTitle("");
    setCompleted(false);
    setIsModalOpen(false);
  }
};
```

### Ошибка 5: ESLint warning "missing dependency"

```
React Hook useCallback has a missing dependency: 'todos'
```

**Причина:** Используете `todos` внутри callback, но не добавили в зависимости

**Решение:**
```typescript
const addTask = useCallback(
  (title: string, completed: boolean) => {
    // ...используем todos
  },
  [todos]  // Добавьте todos в массив зависимостей
);
```

### Ошибка 6: Задача не удаляется

**Причина:** Не передали `onDelete` через props

**Решение:** Проверьте цепочку передачи:
1. TasksFetch создает `deleteTask` ✓
2. TasksFetch передает в `<TaskList onDelete={deleteTask} />` ✓
3. TaskList получает в параметрах `{ onDelete }` ✓
4. TaskList передает в `<Task onDelete={onDelete} />` ✓
5. Task получает в параметрах `{ onDelete }` ✓
6. Task вызывает в onClick `() => onDelete(task.id)` ✓

---

## Дополнительные улучшения (необязательно)

После выполнения основной задачи можете попробовать:

### 1. Красивое кастомное модальное окно подтверждения

Вместо `window.confirm()` создайте собственный компонент:

```typescript
interface ConfirmModalProps {
  title: string;
  message: string;
  onConfirm: () => void;
  onCancel: () => void;
}

function ConfirmModal({ title, message, onConfirm, onCancel }: ConfirmModalProps) {
  return (
    <div className={classes.confirmModal}>
      <div className={classes.confirmContent}>
        <h3>{title}</h3>
        <p>{message}</p>
        <div className={classes.buttons}>
          <button onClick={onConfirm}>Удалить</button>
          <button onClick={onCancel}>Отмена</button>
        </div>
      </div>
    </div>
  );
}
```

### 2. Сохранение в localStorage

Задачи исчезают при перезагрузке страницы. Добавьте сохранение:

```typescript
// Сохранение
useEffect(() => {
  localStorage.setItem('todos', JSON.stringify(todos));
}, [todos]);

// Загрузка
useEffect(() => {
  const saved = localStorage.getItem('todos');
  if (saved) {
    setTodos(JSON.parse(saved));
  }
}, []);
```

### 3. Анимация появления/исчезновения

Добавьте плавные анимации с помощью CSS transitions или библиотеки `framer-motion`.

### 4. Поле описания для задачи

Расширьте интерфейс `Todo` и добавьте текстовое поле для подробного описания.

### 5. Категории задач

Добавьте возможность группировать задачи по категориям (Работа, Личное, и т.д.)

### 6. Поиск и фильтрация

Добавьте строку поиска для фильтрации задач по названию.

### 7. Drag & Drop

Реализуйте перетаскивание задач между списками с помощью библиотеки `react-beautiful-dnd`.

---

## Полезные ресурсы

- [React Hooks](https://react.dev/reference/react) - официальная документация по хукам
- [TypeScript Handbook](https://www.typescriptlang.org/docs/handbook/intro.html) - руководство по TypeScript
- [JavaScript Array Methods](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array) - методы массивов
- [CSS Modules](https://github.com/css-modules/css-modules) - документация по CSS модулям
- [Event Handling in React](https://react.dev/learn/responding-to-events) - обработка событий

---

## Чек-лист выполнения

### Добавление задач:
- [ ] Создана функция `addTask` в `TasksFetch.tsx`
- [ ] Функция использует `useCallback` с правильными зависимостями
- [ ] Создан компонент `AddBtn` с модальным окном
- [ ] Модальное окно содержит поле для названия
- [ ] Модальное окно содержит чекбокс для выбора статуса (активная/завершенная)
- [ ] Функция `addTask` передана в компонент `AddBtn`
- [ ] Форма очищается после добавления задачи
- [ ] Модальное окно закрывается по клику на фон
- [ ] Модальное окно закрывается по кнопке "Отмена"
- [ ] Задача добавляется по нажатию Enter
- [ ] Пустые задачи не добавляются
- [ ] Новые задачи появляются в правильном списке

### Удаление задач:
- [ ] Создана функция `deleteTask` в `TasksFetch.tsx`
- [ ] Функция использует `useCallback` с правильными зависимостями
- [ ] Добавлен параметр `onDelete` в интерфейс `TaskListProps`
- [ ] Параметр `onDelete` получен в компоненте `TaskList`
- [ ] Добавлен параметр `onDelete` в интерфейс `TaskProps`
- [ ] Параметр `onDelete` получен в компоненте `Task`
- [ ] Добавлена кнопка удаления в компонент `Task`
- [ ] Кнопка удаления имеет правильные стили
- [ ] При клике на кнопку показывается подтверждение
- [ ] Задача удаляется только после подтверждения
- [ ] Функция `deleteTask` передана через `TaskList` в `Task`

### Общее:
- [ ] Приложение работает корректно
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в линтере (`npm run lint`)
- [ ] Нет ошибок TypeScript (`npx tsc --noEmit`)
- [ ] Все типы правильно указаны
- [ ] Код чистый и читаемый
- [ ] Комментарии добавлены где нужно

Удачи в изучении React! 🚀
