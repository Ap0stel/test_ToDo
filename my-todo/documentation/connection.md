# Интеграция React приложения с JSONPlaceholder API

## Для кого этот документ

Этот гайд предназначен для разработчиков, изучающих React и работу с REST API. Мы подробно разберем, как превратить приложение с локальным состоянием в полноценное приложение, работающее с сервером.

---

## Содержание

1. [Текущее состояние и проблемы](#1-текущее-состояние-и-проблемы)
2. [Что такое REST API и зачем оно нужно](#2-что-такое-rest-api-и-зачем-оно-нужно)
3. [Создание API модуля](#3-создание-api-модуля)
4. [Интеграция с компонентом TasksFetch](#4-интеграция-с-компонентом-tasksfetch)
5. [Понимание async/await](#5-понимание-asyncawait)
6. [Понимание useCallback](#6-понимание-usecallback)
7. [Опциональные улучшения](#7-опциональные-улучшения)
8. [Тестирование](#8-тестирование)
9. [Частые ошибки](#9-частые-ошибки)
10. [Заключение](#10-заключение)

---

## 1. Текущее состояние и проблемы

### Как работает приложение сейчас

```
                ┌─────────────────────┐
                │  Запуск приложения  │
                └──────────┬──────────┘
                           │
                           ▼
                ┌─────────────────────┐
                │ fetch GET /todos    │ ◄── Загружаем данные с API
                └──────────┬──────────┘
                           │
                           ▼
                ┌─────────────────────┐
                │  Сохраняем в state  │
                │  useState([todos])  │
                └──────────┬──────────┘
                           │
         ┌─────────────────┼─────────────────┐
         ▼                 ▼                 ▼
    ┌────────┐        ┌────────┐       ┌────────┐
    │ Создать│        │Изменить│       │Удалить │
    │ задачу │        │ задачу │       │ задачу │
    └────┬───┘        └────┬───┘       └────┬───┘
         │                 │                 │
         └─────────────────┼─────────────────┘
                           ▼
              ⚠️ ТОЛЬКО ЛОКАЛЬНО В STATE ⚠️
              (не сохраняется на сервере)
```

### Проблема

После первой загрузки все операции (создание, изменение, удаление) выполняются **только локально**. При перезагрузке страницы все изменения теряются, потому что они не отправляются на сервер.

### Цель

Сделать так, чтобы **ВСЕ** операции работали с сервером:

```
┌─────────────┐
│   Браузер   │
│  (React UI) │
└──────┬──────┘
       │
       │ ◄── Все операции идут через API
       │
       ▼
┌─────────────┐
│  API Сервер │
│ (JSONPlace- │
│  holder)    │
└─────────────┘
```

---

## 2. Что такое REST API и зачем оно нужно

### REST API простыми словами

Представь, что API - это официант в ресторане:

- **Ты (Frontend)** - клиент, который хочет заказать еду
- **Официант (API)** - посредник между тобой и кухней
- **Кухня (Backend/Database)** - место, где готовится и хранится еда

REST API использует **HTTP методы** для разных действий:

| HTTP Метод | Аналогия             | Что делает                | Пример                       |
| ---------- | -------------------- | ------------------------- | ---------------------------- |
| GET        | "Покажи мне меню"    | Получить данные           | Загрузить список задач       |
| POST       | "Принеси мне пиццу"  | Создать новые данные      | Создать новую задачу         |
| PATCH      | "Добавь больше сыра" | Обновить часть данных     | Изменить title или completed |
| PUT        | "Замени это блюдо"   | Полностью заменить данные | Заменить всю задачу          |
| DELETE     | "Убери это со стола" | Удалить данные            | Удалить задачу               |

### CRUD операции

CRUD - это аббревиатура основных операций с данными:

- **C**reate (Создать) - POST
- **R**ead (Прочитать) - GET
- **U**pdate (Обновить) - PATCH/PUT
- **D**elete (Удалить) - DELETE

### JSONPlaceholder

`https://jsonplaceholder.typicode.com` - это **фейковый** REST API для тестирования и обучения. Он эмулирует настоящий сервер, но **не сохраняет** изменения. Идеален для практики!

**Эндпоинты (URL для запросов):**

```
GET    /todos          - Получить все задачи
GET    /todos/1        - Получить задачу с id=1
POST   /todos          - Создать новую задачу
PATCH  /todos/1        - Обновить задачу с id=1
DELETE /todos/1        - Удалить задачу с id=1
```

---

## 3. Создание API модуля

### Зачем выносить API в отдельный файл?

**Принцип разделения ответственности (Separation of Concerns):**

- Компоненты React занимаются **UI и логикой отображения**
- API модуль занимается **общением с сервером**

**Преимущества:**

1. Код легче читать и понимать
2. Легко переиспользовать функции API в разных компонентах
3. Легко поменять API (например, на реальный backend)
4. Легко тестировать отдельно

### Шаг 1: Создать папку api

```bash
mkdir src/api
```

### Шаг 2: Создать файл src/api/todos.ts

Создай новый файл `src/api/todos.ts` и скопируй туда следующий код:

```typescript
// Базовый URL API - можно легко поменять на другой сервер
const API_BASE_URL = "https://jsonplaceholder.typicode.com";

// Интерфейс описывает структуру объекта Todo
// TypeScript будет проверять, что мы используем правильные поля
export interface Todo {
  userId: number; // ID пользователя, которому принадлежит задача
  id: number; // Уникальный ID задачи
  title: string; // Название задачи
  completed: boolean; // Статус выполнения
}

/**
 * Получить список всех задач с сервера
 *
 * @returns Promise<Todo[]> - Промис, который вернет массив задач
 */
export async function fetchTodos(): Promise<Todo[]> {
  // Отправляем GET запрос на сервер
  const response = await fetch(`${API_BASE_URL}/todos`);

  // Проверяем, успешен ли запрос (status code 200-299)
  if (!response.ok) {
    // Если запрос не успешен, выбрасываем ошибку
    // Она будет поймана в блоке catch
    throw new Error(`Failed to fetch todos: ${response.statusText}`);
  }

  // Преобразуем JSON ответ в JavaScript объект
  return response.json();
}

/**
 * Создать новую задачу на сервере
 *
 * @param title - Название новой задачи
 * @param userId - ID пользователя (по умолчанию 1)
 * @returns Promise<Todo> - Промис, который вернет созданную задачу с присвоенным ID
 */
export async function createTodo(
  title: string,
  userId: number = 1
): Promise<Todo> {
  // Отправляем POST запрос с данными в теле запроса
  const response = await fetch(`${API_BASE_URL}/todos`, {
    method: "POST",
    headers: {
      // Указываем, что отправляем JSON
      "Content-Type": "application/json",
    },
    // Преобразуем JavaScript объект в JSON строку
    body: JSON.stringify({
      userId,
      title,
      completed: false,
    }),
  });

  if (!response.ok) {
    throw new Error(`Failed to create todo: ${response.statusText}`);
  }

  // Сервер вернет созданный объект с присвоенным ID
  return response.json();
}

/**
 * Обновить существующую задачу (частичное обновление)
 *
 * PATCH vs PUT:
 * - PATCH - обновляет только указанные поля (используем это)
 * - PUT - заменяет весь объект целиком
 *
 * @param id - ID задачи для обновления
 * @param updates - Объект с полями для обновления
 * @returns Promise<Todo> - Промис, который вернет обновленную задачу
 */
export async function updateTodo(
  id: number,
  updates: Partial<Pick<Todo, "title" | "completed">>
): Promise<Todo> {
  // Разбор типа:
  // Pick<Todo, 'title' | 'completed'> - выбираем только поля title и completed из Todo
  // Partial<...> - делаем все поля необязательными
  // Итог: { title?: string, completed?: boolean }

  const response = await fetch(`${API_BASE_URL}/todos/${id}`, {
    method: "PATCH",
    headers: {
      "Content-Type": "application/json",
    },
    body: JSON.stringify(updates),
  });

  if (!response.ok) {
    throw new Error(`Failed to update todo: ${response.statusText}`);
  }

  return response.json();
}

/**
 * Удалить задачу с сервера
 *
 * @param id - ID задачи для удаления
 * @returns Promise<void> - Промис без возвращаемого значения
 */
export async function deleteTodo(id: number): Promise<void> {
  const response = await fetch(`${API_BASE_URL}/todos/${id}`, {
    method: "DELETE",
  });

  if (!response.ok) {
    throw new Error(`Failed to delete todo: ${response.statusText}`);
  }

  // DELETE обычно не возвращает данные, только статус код
  // Поэтому не вызываем response.json()
}
```

### Что происходит в этом коде?

#### Fetch API

`fetch()` - встроенная в браузер функция для HTTP запросов.

```typescript
fetch(url, options);
```

**Параметры:**

- `url` - куда отправляем запрос
- `options` - объект с настройками (method, headers, body)

**Возвращает:** Promise с объектом Response

#### Async/Await

```typescript
async function myFunction() {
  const result = await someAsyncOperation();
  // код подождет, пока someAsyncOperation завершится
}
```

- `async` - помечает функцию как асинхронную
- `await` - приостанавливает выполнение до завершения промиса

Подробнее в разделе [5. Понимание async/await](#5-понимание-asyncawait)

#### Promise<T>

Promise - это обещание получить значение в будущем.

```typescript
Promise<Todo[]>; // Обещание получить массив Todo
Promise<Todo>; // Обещание получить один Todo
Promise<void>; // Обещание без возвращаемого значения
```

#### TypeScript типы

```typescript
Partial<T>; // Делает все поля необязательными
Pick<T, K>; // Выбирает только указанные поля
```

**Пример:**

```typescript
interface Todo {
  id: number;
  title: string;
  completed: boolean;
}

type Updates = Partial<Pick<Todo, "title" | "completed">>;
// Результат: { title?: string, completed?: boolean }
```

---

## 4. Интеграция с компонентом TasksFetch

Теперь обновим компонент `TasksFetch.tsx`, чтобы он использовал наши API функции.

### Шаг 1: Импортировать API функции

Добавь в начало файла `TasksFetch.tsx`:

```typescript
import {
  fetchTodos,
  createTodo,
  updateTodo,
  deleteTodo,
} from "../../api/todos";
```

### Шаг 2: Заменить useEffect для загрузки задач

**Найди в файле (строки 15-26):**

```typescript
useEffect(() => {
  fetch("https://jsonplaceholder.typicode.com/todos")
    .then((result) => result.json())
    .then((toDos: Todo[]) => {
      setTodos(toDos.slice(0, 20));
      setIsLoadingTasks(false);
    })
    .catch(() => {
      setError("Ошибка в получении контента");
      setIsLoadingTasks(false);
    });
}, []);
```

**Замени на:**

```typescript
useEffect(() => {
  fetchTodos()
    .then((toDos) => {
      setTodos(toDos.slice(0, 20));
      setIsLoadingTasks(false);
    })
    .catch((error) => {
      console.error("Error fetching todos:", error);
      setError("Ошибка в получении контента");
      setIsLoadingTasks(false);
    });
}, []);
```

**Что изменилось?**

- Вместо `fetch("https://...")` используем `fetchTodos()`
- В catch добавили `console.error` для отладки

**Почему это лучше?**

- Код проще и понятнее
- Логика работы с API скрыта в модуле
- Легко поменять URL или логику запроса в одном месте

---

### Шаг 3: Обновить функцию createTask

**Найди в файле (строки 72-85):**

```typescript
const createTask = () => {
  if (newTaskTitle.trim() === "") return;

  const newTask = {
    id: Date.now(),
    userId: 1,
    title: newTaskTitle,
    completed: false,
  };

  setTodos((actualTodos) => [newTask, ...actualTodos]);
  setNewTaskTitle("");
  setIsCreating(false);
};
```

**Замени на:**

```typescript
const createTask = async () => {
  if (newTaskTitle.trim() === "") return;

  try {
    // Отправляем запрос на сервер
    const newTask = await createTodo(newTaskTitle.trim());

    // Добавляем задачу в начало списка
    setTodos((actualTodos) => [newTask, ...actualTodos]);

    // Очищаем поле ввода
    setNewTaskTitle("");

    // Закрываем форму создания
    setIsCreating(false);
  } catch (error) {
    console.error("Error creating task:", error);
    setError("Ошибка при создании задачи");
  }
};
```

**Что изменилось?**

1. Добавили `async` перед функцией
2. Обернули код в `try/catch` для обработки ошибок
3. Используем `await createTodo()` вместо создания объекта локально
4. При ошибке устанавливаем сообщение об ошибке

**Почему `async`?**

Теперь функция выполняет асинхронную операцию (запрос к серверу), поэтому нужно пометить её как `async`.

**ВАЖНО:** Также нужно обновить `handleCreateKeyDown`:

**Найди:**

```typescript
const handleCreateKeyDown = (e: React.KeyboardEvent<HTMLInputElement>) => {
  if (e.key === "Enter") {
    createTask();
  }
};
```

**Замени на:**

```typescript
const handleCreateKeyDown = async (
  e: React.KeyboardEvent<HTMLInputElement>
) => {
  if (e.key === "Enter") {
    await createTask();
  }
};
```

---

### Шаг 4: Обновить функцию toggleTask

**Найди в файле (строки 42-51):**

```typescript
const toggleTask = useCallback(
  (taskId: number) => {
    const updatedTodos = todos.map((task) =>
      task.id === taskId ? { ...task, completed: !task.completed } : task
    );

    setTodos(updatedTodos);
  },
  [todos]
);
```

**Замени на:**

```typescript
const toggleTask = useCallback(
  async (taskId: number) => {
    // Находим задачу, которую нужно обновить
    const task = todos.find((t) => t.id === taskId);
    if (!task) return; // Если не нашли, выходим

    try {
      // Отправляем обновление на сервер
      await updateTodo(taskId, { completed: !task.completed });

      // Обновляем локальный state только после успешного ответа
      const updatedTodos = todos.map((t) =>
        t.id === taskId ? { ...t, completed: !t.completed } : t
      );
      setTodos(updatedTodos);
    } catch (error) {
      console.error("Error toggling task:", error);
      setError("Ошибка при изменении статуса задачи");
    }
  },
  [todos]
);
```

**Что изменилось?**

1. Добавили `async` перед параметрами
2. Сначала ищем задачу с помощью `find()`
3. Отправляем PATCH запрос на сервер
4. Обновляем state **после** успешного ответа
5. Ловим ошибки в `catch`

**Порядок важен!**

```
1. Отправить запрос на сервер
2. Дождаться ответа
3. Обновить UI

А не наоборот!
```

---

### Шаг 5: Обновить функцию updateTaskTitle

**Найди в файле (строки 53-65):**

```typescript
const updateTaskTitle = useCallback(
  (taskId: number, newTitle: string) => {
    if (newTitle.trim() === "") {
      return;
    }

    const updTodos = todos.map((task) =>
      task.id === taskId ? { ...task, title: newTitle.trim() } : task
    );
    setTodos(updTodos);
  },
  [todos]
);
```

**Замени на:**

```typescript
const updateTaskTitle = useCallback(
  async (taskId: number, newTitle: string) => {
    if (newTitle.trim() === "") {
      return;
    }

    try {
      // Отправляем обновленный title на сервер
      await updateTodo(taskId, { title: newTitle.trim() });

      // Обновляем локальный state
      const updTodos = todos.map((task) =>
        task.id === taskId ? { ...task, title: newTitle.trim() } : task
      );
      setTodos(updTodos);
    } catch (error) {
      console.error("Error updating task title:", error);
      setError("Ошибка при редактировании задачи");
    }
  },
  [todos]
);
```

**Аналогично предыдущему шагу:**

- Сначала отправляем на сервер
- Потом обновляем UI
- Обрабатываем ошибки

---

### Шаг 6: Обновить функцию deleteTask

**Найди в файле (строки 68-70):**

```typescript
const deleteTask = (taskId: number) => {
  setTodos((actualTodos) => actualTodos.filter((task) => task.id != taskId));
};
```

**Замени на:**

```typescript
const deleteTask = useCallback(async (taskId: number) => {
  try {
    // Отправляем DELETE запрос на сервер
    await deleteTodo(taskId);

    // Удаляем из локального state
    setTodos((actualTodos) => actualTodos.filter((task) => task.id !== taskId));
  } catch (error) {
    console.error("Error deleting task:", error);
    setError("Ошибка при удалении задачи");
  }
}, []);
```

**Что изменилось?**

1. Обернули в `useCallback` для консистентности с другими функциями
2. Добавили `async`
3. Сначала удаляем на сервере, потом в UI
4. Используем `!==` вместо `!=` (строгое сравнение)

**Зависимости useCallback:**

Заметь, что массив зависимостей пустой `[]`, потому что функция использует функциональную форму `setTodos`, которая не зависит от текущего значения `todos`.

---

## 5. Понимание async/await

### Зачем нужен async/await?

JavaScript - однопоточный язык. Когда мы отправляем запрос к серверу, он может занять время (сеть медленная). Мы не хотим блокировать весь интерфейс на это время.

### Синхронный код (блокирующий)

```javascript
// ❌ Плохо - это заблокирует весь интерфейс
const data = downloadHugeFile(); // Ждем 10 секунд...
console.log(data);
// UI зависнет на 10 секунд
```

### Промисы (старый способ)

```javascript
// ✓ Хорошо, но код становится сложным
fetch("/api/todos")
  .then((response) => response.json())
  .then((data) => {
    console.log(data);
    return fetch("/api/users");
  })
  .then((response) => response.json())
  .then((users) => {
    console.log(users);
  })
  .catch((error) => {
    console.error(error);
  });
// Лестница промисов (Promise Hell)
```

### Async/Await (современный способ)

```javascript
// ✓✓ Отлично - читается как синхронный код!
async function loadData() {
  try {
    const todosResponse = await fetch("/api/todos");
    const todos = await todosResponse.json();
    console.log(todos);

    const usersResponse = await fetch("/api/users");
    const users = await usersResponse.json();
    console.log(users);
  } catch (error) {
    console.error(error);
  }
}
```

### Правила async/await

1. `await` можно использовать **только** внутри `async` функции
2. `async` функция **всегда** возвращает Promise
3. Ошибки ловим через `try/catch`

### В React

```typescript
// ❌ Плохо - useEffect НЕ может быть async
useEffect(async () => {
  const data = await fetchData();
}, []);

// ✓ Хорошо - создаем async функцию внутри
useEffect(() => {
  async function loadData() {
    const data = await fetchData();
    setData(data);
  }
  loadData();
}, []);

// ✓ Хорошо - используем .then()
useEffect(() => {
  fetchData().then((data) => {
    setData(data);
  });
}, []);
```

---

## 6. Понимание useCallback

### Что такое useCallback?

`useCallback` - это React хук, который **мемоизирует** (кэширует) функцию.

### Зачем это нужно?

В JavaScript при каждом рендере компонента создаются **новые** функции:

```javascript
function MyComponent() {
  // При каждом рендере создается НОВАЯ функция
  const handleClick = () => {
    console.log("clicked");
  };

  return <Button onClick={handleClick} />;
}
```

**Проблема:**

Если `Button` обернут в `React.memo`, он будет перерендериваться каждый раз, потому что `handleClick` - это **новая** функция (новая ссылка в памяти).

### Решение с useCallback

```javascript
function MyComponent() {
  // Функция создается только один раз
  const handleClick = useCallback(() => {
    console.log("clicked");
  }, []); // пустой массив = создать один раз

  return <Button onClick={handleClick} />;
}
```

### Массив зависимостей

```typescript
const someFunction = useCallback(
  () => {
    // код функции
  },
  [dep1, dep2] // если dep1 или dep2 изменятся, функция пересоздастся
);
```

### В нашем случае

```typescript
const toggleTask = useCallback(
  async (taskId: number) => {
    const task = todos.find((t) => t.id === taskId);
    // ...
  },
  [todos] // Функция пересоздается, когда меняется todos
);
```

**Почему `[todos]`?**

Потому что внутри функции мы используем переменную `todos`. Если `todos` изменится, нам нужна новая версия функции с актуальным значением `todos`.

### Функциональная форма setState

```typescript
// ❌ Зависит от todos
setTodos(todos.filter((t) => t.id !== taskId));

// ✓ НЕ зависит от todos
setTodos((actualTodos) => actualTodos.filter((t) => t.id !== taskId));
```

Во втором случае React передаст **актуальное** значение `todos` в функцию, поэтому зависимость не нужна:

```typescript
const deleteTask = useCallback(
  async (taskId: number) => {
    await deleteTodo(taskId);
    setTodos((actualTodos) => actualTodos.filter((t) => t.id !== taskId));
  },
  [] // Пустой массив - не зависит от todos
);
```

---

## 7. Опциональные улучшения

Эти улучшения не обязательны, но сделают приложение лучше.

### 7.1 Индикаторы загрузки

Показывать пользователю, что операция выполняется.

```typescript
// Добавить новые состояния
const [isCreatingTask, setIsCreatingTask] = useState<boolean>(false);

const createTask = async () => {
  if (newTaskTitle.trim() === "") return;

  setIsCreatingTask(true); // Начинаем загрузку

  try {
    const newTask = await createTodo(newTaskTitle.trim());
    setTodos((actualTodos) => [newTask, ...actualTodos]);
    setNewTaskTitle("");
    setIsCreating(false);
  } catch (error) {
    console.error("Error creating task:", error);
    setError("Ошибка при создании задачи");
  } finally {
    setIsCreatingTask(false); // Заканчиваем загрузку (в любом случае)
  }
};

// В JSX
<button onClick={() => setIsCreating(true)} disabled={isCreatingTask}>
  {isCreatingTask ? "Создание..." : "+ Добавить задачу"}
</button>;
```

**Finally блок:**

Выполняется **всегда**, даже если была ошибка. Идеально для очистки состояния загрузки.

### 7.2 Оптимистичные обновления (Optimistic UI)

Обновляем UI **сразу**, не дожидаясь ответа сервера. Если сервер вернет ошибку - откатываем изменения.

**Преимущества:**

- Приложение кажется быстрым
- Лучший UX

**Недостатки:**

- Сложнее код
- Нужно откатывать изменения при ошибках

```typescript
const deleteTask = useCallback(
  async (taskId: number) => {
    // Сохраняем удаляемую задачу
    const deletedTask = todos.find((t) => t.id === taskId);
    if (!deletedTask) return;

    // Оптимистично удаляем из UI СРАЗУ
    setTodos((actualTodos) => actualTodos.filter((t) => t.id !== taskId));

    try {
      // Отправляем запрос на сервер
      await deleteTodo(taskId);
    } catch (error) {
      console.error("Error deleting task:", error);

      // ОТКАТЫВАЕМ изменения - возвращаем задачу обратно
      setTodos((actualTodos) => [...actualTodos, deletedTask]);

      setError("Ошибка при удалении задачи");
    }
  },
  [todos]
);
```

**Когда использовать:**

- Для быстрых операций (delete, toggle)
- Когда вероятность ошибки низкая
- Когда важна скорость отклика UI

**Когда НЕ использовать:**

- Для важных операций (платежи)
- Когда нужна проверка на сервере
- Когда откат сложен

### 7.3 Автоматическая очистка ошибок

Ошибки исчезают через несколько секунд автоматически.

```typescript
useEffect(() => {
  if (error) {
    // Создаем таймер на 5 секунд
    const timer = setTimeout(() => {
      setError(null);
    }, 5000);

    // Cleanup функция - вызывается при размонтировании или перед следующим эффектом
    return () => clearTimeout(timer);
  }
}, [error]); // Запускается при изменении error
```

**Как это работает:**

1. Когда `error` появляется - запускаем таймер
2. Через 5 секунд очищаем `error`
3. Если `error` изменится до истечения таймера - отменяем старый таймер (cleanup)
4. Создаем новый таймер

## 8. Тестирование

### Checklist для тестирования

После внесения всех изменений проверь:

#### ✅ Загрузка задач

1. Открой приложение
2. Должен появиться "Загрузка контента..."
3. Через 1-2 секунды должны появиться первые 20 задач

#### ✅ Создание задачи

1. Нажми "+ Добавить задачу"
2. Введи название, например "Купить молоко"
3. Нажми Enter
4. Задача должна появиться в начале списка "Активные"
5. Открой DevTools → Network → проверь, что был POST запрос

#### ✅ Изменение статуса (completed)

1. Кликни на чекбокс активной задачи
2. Задача должна переместиться в "Завершенные"
3. В DevTools → Network должен быть PATCH запрос

#### ✅ Редактирование названия

1. Кликни на название задачи
2. Измени текст
3. Нажми Enter или кликни вне поля
4. Название должно обновиться
5. В DevTools → Network должен быть PATCH запрос

#### ✅ Удаление задачи

1. Кликни на кнопку "X" у задачи
2. Задача должна исчезнуть
3. В DevTools → Network должен быть DELETE запрос

#### ✅ Обработка ошибок сети

1. Открой DevTools → Network
2. Включи "Offline" режим
3. Попробуй создать/изменить/удалить задачу
4. Должно появиться сообщение об ошибке
5. В консоли должна быть ошибка с описанием

### Как открыть DevTools

- **Chrome/Edge:** F12 или Ctrl+Shift+I (Windows) / Cmd+Option+I (Mac)
- **Firefox:** F12 или Ctrl+Shift+I (Windows) / Cmd+Option+I (Mac)
- **Safari:** Cmd+Option+C (нужно сначала включить в настройках)

### Что смотреть в DevTools

**Network Tab:**

- Видны все HTTP запросы
- Можешь кликнуть на запрос и посмотреть:
  - Request Headers (заголовки запроса)
  - Request Payload (тело запроса)
  - Response (ответ сервера)
  - Status Code (код статуса)

**Console Tab:**

- Все `console.log()` и `console.error()`
- Ошибки JavaScript

---

## 9. Частые ошибки

### Ошибка 1: Забыл добавить async

```typescript
// ❌ Неправильно - нет async
const createTask = () => {
  await createTodo(title); // Синтаксическая ошибка!
};

// ✓ Правильно
const createTask = async () => {
  await createTodo(title);
};
```

### Ошибка 2: Не обработал ошибки

```typescript
// ❌ Неправильно - нет обработки ошибок
const createTask = async () => {
  const task = await createTodo(title);
  // Если запрос упадет - необработанное исключение
};

// ✓ Правильно
const createTask = async () => {
  try {
    const task = await createTodo(title);
  } catch (error) {
    console.error(error);
    setError("Ошибка");
  }
};
```

### Ошибка 3: Обновил UI до запроса

```typescript
// ❌ Неправильно - обновили UI, а сервер может вернуть ошибку
setTodos([...todos, newTask]);
await createTodo(title);

// ✓ Правильно - сначала запрос, потом UI
const newTask = await createTodo(title);
setTodos([...todos, newTask]);
```

### Ошибка 4: Не дождался ответа

```typescript
// ❌ Неправильно - забыл await
const createTask = async () => {
  const task = createTodo(title); // Получили Promise, а не задачу!
  setTodos([...todos, task]); // task - это Promise, не объект Todo
};

// ✓ Правильно
const createTask = async () => {
  const task = await createTodo(title); // Дождались ответа
  setTodos([...todos, task]);
};
```

### Ошибка 5: Неправильные зависимости useCallback

```typescript
// ❌ Неправильно - используем todos, но не указали в зависимостях
const toggleTask = useCallback(
  async (taskId: number) => {
    const task = todos.find((t) => t.id === taskId); // Используем todos
  },
  [] // Пустой массив - React Warning!
);

// ✓ Правильно
const toggleTask = useCallback(
  async (taskId: number) => {
    const task = todos.find((t) => t.id === taskId);
  },
  [todos] // Указали todos в зависимостях
);
```

### Ошибка 6: Забыл проверить response.ok

```typescript
// ❌ Неправильно - не проверяем статус
const response = await fetch("/api/todos");
const data = await response.json(); // Может упасть, если ошибка

// ✓ Правильно
const response = await fetch("/api/todos");
if (!response.ok) {
  throw new Error(`HTTP error! status: ${response.status}`);
}
const data = await response.json();
```

### Ошибка 7: Неправильный Content-Type

```typescript
// ❌ Неправильно - не указали Content-Type
fetch("/api/todos", {
  method: "POST",
  body: JSON.stringify({ title: "Task" }),
});

// ✓ Правильно
fetch("/api/todos", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
  },
  body: JSON.stringify({ title: "Task" }),
});
```

---

## 10. Заключение

### Что мы сделали

1. ✅ Создали отдельный API модуль (`src/api/todos.ts`)
2. ✅ Реализовали все CRUD операции:
   - CREATE (POST) - создание задачи
   - READ (GET) - чтение задач
   - UPDATE (PATCH) - обновление задачи
   - DELETE (DELETE) - удаление задачи
3. ✅ Интегрировали API с React компонентом
4. ✅ Добавили обработку ошибок
5. ✅ Узнали про async/await и useCallback

### Структура проекта

```
src/
├── api/
│   └── todos.ts              # Новый - API функции
├── components/
│   ├── Task/
│   │   ├── Task.tsx
│   │   └── Task.module.css
│   ├── TaskList/
│   │   ├── TaskList.tsx
│   │   └── TaskList.module.css
│   └── TasksFetch/
│       ├── TasksFetch.tsx    # Изменен - интеграция с API
│       └── TasksFetch.module.css
├── types/
│   └── index.ts
├── App.tsx
└── main.tsx
```

### Что дальше?

**Следующие шаги для изучения:**

1. **Изучить React Query / TanStack Query**

   - Автоматический кэш запросов
   - Автоматические повторные запросы
   - Оптимистичные обновления из коробки

2. **Добавить состояние загрузки**

   - Показывать спиннеры
   - Отключать кнопки во время запроса

3. **Добавить валидацию**

   - Проверять длину названия
   - Запрещать пустые задачи

4. **Добавить localStorage**

   - Сохранять изменения локально
   - Синхронизировать с сервером

5. **Изучить TypeScript глубже**
   - Дженерики
   - Utility Types (Partial, Pick, Omit)
   - Type Guards

### Полезные ресурсы

- [MDN Web Docs - Fetch API](https://developer.mozilla.org/ru/docs/Web/API/Fetch_API)
- [MDN - async/await](https://developer.mozilla.org/ru/docs/Learn/JavaScript/Asynchronous/Async_await)
- [React Docs - useCallback](https://react.dev/reference/react/useCallback)
- [React Docs - useEffect](https://react.dev/reference/react/useEffect)
- [JSONPlaceholder Guide](https://jsonplaceholder.typicode.com/guide/)

### Важное напоминание

JSONPlaceholder - это **фейковый** API. Он:

- ✓ Возвращает правильные HTTP ответы
- ✓ Эмулирует задержку сети
- ✓ Идеален для обучения
- ✗ **НЕ сохраняет** изменения на сервере
- ✗ При перезагрузке все изменения теряются

Для реального приложения понадобится настоящий backend.

---

## Checklist выполнения

Используй этот чеклист, чтобы не пропустить шаги:

- [ ] Создать папку `src/api`
- [ ] Создать файл `src/api/todos.ts`
- [ ] Скопировать код API функций в `todos.ts`
- [ ] Открыть `src/components/TasksFetch/TasksFetch.tsx`
- [ ] Добавить импорт API функций
- [ ] Заменить useEffect для загрузки задач
- [ ] Обновить функцию `createTask`
- [ ] Обновить функцию `handleCreateKeyDown`
- [ ] Обновить функцию `toggleTask`
- [ ] Обновить функцию `updateTaskTitle`
- [ ] Обновить функцию `deleteTask`
- [ ] Сохранить все файлы
- [ ] Запустить приложение (`npm run dev`)
- [ ] Протестировать загрузку задач
- [ ] Протестировать создание задачи
- [ ] Протестировать изменение статуса
- [ ] Протестировать редактирование названия
- [ ] Протестировать удаление задачи
- [ ] Проверить Network в DevTools
- [ ] Протестировать обработку ошибок (Offline mode)
- [ ] (Опционально) Добавить индикаторы загрузки
- [ ] (Опционально) Реализовать оптимистичные обновления
- [ ] (Опционально) Добавить автоочистку ошибок

---

**Удачи в изучении React и работе с API!** 🚀

Если что-то непонятно - перечитай соответствующий раздел. Программирование требует практики, не расстраивайся, если что-то не получается с первого раза.
