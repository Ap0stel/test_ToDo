# TODO: Добавить возможность изменения title при клике на него

## Общая концепция

Нужно добавить функционал, который позволит пользователю редактировать название задачи (title) напрямую в списке. При клике на title должен появляться input с текущим значением, который позволит изменить текст. После ввода нового текста и нажатия Enter или потери фокуса (blur), изменения должны сохраняться.

---

## Шаг 1: Понимание текущей архитектуры

### Структура компонентов:
```
App
  └── TasksFetch (управляет состоянием и загрузкой данных)
      └── TaskList (отображает список задач)
          └── Task (отображает одну задачу с title и чекбоксом)
```

### Текущее отображение title:

В компоненте `Task.tsx` (строка 21) title просто отображается как текст:
```typescript
<h3 className={classes["task-info_title"]}>{task.title}</h3>
```

Нам нужно сделать так, чтобы при клике на этот текст появлялся input для редактирования.

---

## Шаг 2: Какое состояние (State) нам нужно?

### Концепция: Локальное vs Глобальное состояние

Нам нужно отслеживать две вещи:
1. **Какая задача сейчас редактируется?** - Чтобы знать, где показывать input вместо текста
2. **Какой текст пользователь вводит?** - Чтобы отображать его в input'е

**Где хранить это состояние?**
- Изменение title задачи должно обновлять массив `todos` в `TasksFetch`
- Но состояние редактирования (какая задача редактируется) можно хранить локально в `Task`

**Почему локально в Task?**
- Одновременно редактируется только одна задача
- Не нужно передавать это состояние другим компонентам
- Проще управлять

---

## Шаг 3: Добавление состояния в компонент Task

### 3.1. Импортируйте хук useState (Task.tsx):

**Где:** В строке 1, в импорте из React

**Было:**
```typescript
import classes from "./Task.module.css";
import { Todo } from "../../types";
```

**Стало:**
```typescript
import { useState } from "react";
import classes from "./Task.module.css";
import { Todo } from "../../types";
```

### 3.2. Добавьте состояния в компонент Task:

**Где:** В самом начале функции `Task`, после объявления параметров (после строки 9)

```typescript
function Task({ task, onToggle }: Readonly<TaskProps>) {
  // Состояние: редактируется ли эта задача?
  const [isEditing, setIsEditing] = useState(false);

  // Состояние: временное значение title во время редактирования
  const [editingTitle, setEditingTitle] = useState(task.title);

  return (
    // ... остальной код
  );
}
```

**Объяснение:**

1. **`isEditing`** - булево значение (true/false)
   - `true` = показываем input для редактирования
   - `false` = показываем обычный текст

2. **`editingTitle`** - строка с текстом
   - Начальное значение = `task.title` (текущий title задачи)
   - Обновляется при вводе текста в input

---

## Шаг 4: Создание функции для изменения title в TasksFetch

### 4.1. Добавьте функцию updateTaskTitle в TasksFetch.tsx:

**Где:** После функции `toggleTask`, примерно на строке 45

```typescript
const updateTaskTitle = useCallback(
  (taskId: number, newTitle: string) => {
    // Проверяем, что новый title не пустой
    if (newTitle.trim() === "") {
      return; // Если пустой - не обновляем
    }

    // Создаем новый массив с обновленной задачей
    setTodos(todos.map(todo =>
      todo.id === taskId
        ? { ...todo, title: newTitle.trim() }  // Обновляем title
        : todo  // Оставляем остальные задачи без изменений
    ));
  },
  [todos]
);
```

**Что такое useCallback?**

`useCallback` - это хук React для мемоизации функций. Он сохраняет одну и ту же функцию между рендерами, пока не изменятся зависимости.

**Синтаксис:**
```typescript
const memoizedFunction = useCallback(
  () => { /* код функции */ },
  [dependency1, dependency2]  // Массив зависимостей
);
```

**Зачем это нужно?**
- В JavaScript каждый раз при рендере компонента создаются новые функции
- Если передать новую функцию в props, дочерний компонент перерисуется
- `useCallback` возвращает ту же функцию, если зависимости не изменились
- Это оптимизирует производительность

**Объяснение функции построчно:**

1. `newTitle.trim()` - удаляет пробелы в начале и конце строки
2. `newTitle.trim() === ""` - проверяем, не пустая ли строка после удаления пробелов
3. `return;` - если пустая, выходим из функции (не обновляем)
4. `todos.map()` - создаем новый массив задач
5. `todo.id === taskId` - находим нужную задачу
6. `{ ...todo, title: newTitle.trim() }` - создаем новый объект задачи с обновленным title

### 4.2. Не забудьте импортировать useCallback:

**Где:** В строке 1, в импорте из React

**Было:**
```typescript
import { useCallback, useEffect, useMemo, useState } from "react";
```

Если `useCallback` уже есть в импорте (как в нашем случае), ничего добавлять не нужно.

---

## Шаг 5: Передача функции через props

### 5.1. Передайте функцию в TaskList (TasksFetch.tsx):

**Где:** В строках 52-57, где рендерятся компоненты `<TaskList>`

**Было:**
```typescript
<TaskList title="Активные" tasks={activeTasks} onToggle={toggleTask} />
<TaskList
  title="Завершенные"
  tasks={completedTasks}
  onToggle={toggleTask}
/>
```

**Стало:**
```typescript
<TaskList
  title="Активные"
  tasks={activeTasks}
  onToggle={toggleTask}
  onUpdateTitle={updateTaskTitle}  // Добавили новый prop
/>
<TaskList
  title="Завершенные"
  tasks={completedTasks}
  onToggle={toggleTask}
  onUpdateTitle={updateTaskTitle}  // Добавили новый prop
/>
```

### 5.2. Обновите типы в TaskList.tsx:

**Где:** В интерфейсе `TaskListProps`

Сначала прочитайте TaskList.tsx, чтобы увидеть текущую структуру:

**Было:**
```typescript
interface TaskListProps {
  tasks: Todo[];
  title: string;
  onToggle: (id: number) => void;
}
```

**Стало:**
```typescript
interface TaskListProps {
  tasks: Todo[];
  title: string;
  onToggle: (id: number) => void;
  onUpdateTitle: (taskId: number, newTitle: string) => void;  // Новая функция
}
```

**Что это значит?**
- `(taskId: number, newTitle: string)` - функция принимает два параметра:
  - `taskId` - число (id задачи)
  - `newTitle` - строка (новое название)
- `=> void` - функция ничего не возвращает

### 5.3. Получите функцию в параметрах TaskList:

**Где:** В деструктуризации параметров функции `TaskList`

**Было:**
```typescript
export default function TaskList({ tasks, title, onToggle }: TaskListProps) {
```

**Стало:**
```typescript
export default function TaskList({ tasks, title, onToggle, onUpdateTitle }: TaskListProps) {
```

### 5.4. Передайте функцию в Task:

**Где:** Где рендерится компонент `<Task>` (в map)

**Было:**
```typescript
tasks.map((task) => {
  return <Task
    task={task}
    key={task.id}
    onToggle={onToggle}
  />
})
```

**Стало:**
```typescript
tasks.map((task) => {
  return <Task
    task={task}
    key={task.id}
    onToggle={onToggle}
    onUpdateTitle={onUpdateTitle}  // Прокидываем функцию дальше
  />
})
```

---

## Шаг 6: Обновление компонента Task

### 6.1. Обновите интерфейс TaskProps (Task.tsx):

**Где:** В интерфейсе `TaskProps`

**Было:**
```typescript
interface TaskProps {
  task: Todo;
  onToggle: (id: number) => void;
}
```

**Стало:**
```typescript
interface TaskProps {
  task: Todo;
  onToggle: (id: number) => void;
  onUpdateTitle: (taskId: number, newTitle: string) => void;
}
```

### 6.2. Получите функцию в параметрах Task:

**Где:** В строке параметров функции

**Было:**
```typescript
function Task({ task, onToggle }: Readonly<TaskProps>) {
```

**Стало:**
```typescript
function Task({ task, onToggle, onUpdateTitle }: Readonly<TaskProps>) {
```

### 6.3. Создайте вспомогательные функции в Task:

**Где:** После объявления состояний, перед return

```typescript
function Task({ task, onToggle, onUpdateTitle }: Readonly<TaskProps>) {
  const [isEditing, setIsEditing] = useState(false);
  const [editingTitle, setEditingTitle] = useState(task.title);

  // Функция для начала редактирования
  const handleStartEditing = () => {
    setIsEditing(true);
    setEditingTitle(task.title); // Устанавливаем текущий title в input
  };

  // Функция для сохранения изменений
  const handleSaveTitle = () => {
    onUpdateTitle(task.id, editingTitle); // Вызываем функцию из props
    setIsEditing(false); // Выходим из режима редактирования
  };

  // Функция для отмены редактирования
  const handleCancelEditing = () => {
    setEditingTitle(task.title); // Возвращаем исходное значение
    setIsEditing(false); // Выходим из режима редактирования
  };

  // Обработчик нажатия клавиш
  const handleKeyDown = (e: React.KeyboardEvent<HTMLInputElement>) => {
    if (e.key === "Enter") {
      handleSaveTitle(); // Enter - сохраняем
    } else if (e.key === "Escape") {
      handleCancelEditing(); // Escape - отменяем
    }
  };

  return (
    // ... JSX код
  );
}
```

**Объяснение функций:**

1. **`handleStartEditing`**:
   - Устанавливает `isEditing` в `true` → показывается input
   - Устанавливает `editingTitle` в текущий `task.title` → input заполняется

2. **`handleSaveTitle`**:
   - Вызывает `onUpdateTitle` с id задачи и новым title
   - Устанавливает `isEditing` в `false` → возвращается обычный текст

3. **`handleCancelEditing`**:
   - Возвращает `editingTitle` к исходному значению
   - Устанавливает `isEditing` в `false`

4. **`handleKeyDown`**:
   - Обрабатывает нажатия клавиш на клавиатуре
   - `e.key === "Enter"` - пользователь нажал Enter → сохраняем
   - `e.key === "Escape"` - пользователь нажал Escape → отменяем

**Что такое React.KeyboardEvent?**
- Это TypeScript тип для событий клавиатуры в React
- `<HTMLInputElement>` указывает, что событие происходит на элементе input

### 6.4. Обновите JSX для отображения title:

**Где:** В div с классом "task-info", где сейчас находится `<h3>` с title (строка 20-22)

**Было:**
```typescript
<div className={classes["task-info"]}>
  <h3 className={classes["task-info_title"]}>{task.title}</h3>
</div>
```

**Стало:**
```typescript
<div className={classes["task-info"]}>
  {isEditing ? (
    <input
      type="text"
      className={classes["task-info_title-input"]}
      value={editingTitle}
      onChange={(e) => setEditingTitle(e.target.value)}
      onBlur={handleSaveTitle}
      onKeyDown={handleKeyDown}
      autoFocus
    />
  ) : (
    <h3
      className={classes["task-info_title"]}
      onClick={handleStartEditing}
    >
      {task.title}
    </h3>
  )}
</div>
```

**Объяснение построчно:**

1. **`{isEditing ? ... : ...}`** - тернарный оператор (условный рендеринг)
   - Если `isEditing === true` → показываем input
   - Если `isEditing === false` → показываем h3

2. **Input (режим редактирования):**
   - `value={editingTitle}` - контролируемый input, значение из state
   - `onChange={(e) => setEditingTitle(e.target.value)}` - обновляем state при вводе
     - `e.target.value` - текущее значение в input
   - `onBlur={handleSaveTitle}` - сохраняем при потере фокуса (клик вне input)
   - `onKeyDown={handleKeyDown}` - обрабатываем Enter и Escape
   - `autoFocus` - автоматически фокусируется при появлении

3. **h3 (обычный режим):**
   - `onClick={handleStartEditing}` - при клике начинаем редактирование
   - `{task.title}` - отображаем текущий title

---

## Шаг 7: Добавление стилей для input

### Концепция: CSS Modules

В проекте используются CSS Modules - это способ изоляции стилей. Классы из `.module.css` файлов импортируются как объект и применяются через `classes.className`.

### 7.1. Откройте Task.module.css и добавьте стили:

**Где:** В конец файла `Task.module.css`

```css
.task-info_title {
  cursor: pointer; /* Показываем, что на элемент можно кликнуть */
  margin: 0;
  padding: 4px;
  border-radius: 4px;
  transition: background-color 0.2s;
}

.task-info_title:hover {
  background-color: rgba(0, 0, 0, 0.05); /* Легкая подсветка при наведении */
}

.task-info_title-input {
  width: 100%;
  padding: 4px;
  font-size: 1rem;
  font-family: inherit; /* Наследуем шрифт от родителя */
  border: 2px solid #4CAF50; /* Зеленая рамка */
  border-radius: 4px;
  outline: none;
}

.task-info_title-input:focus {
  border-color: #45a049; /* Темнее при фокусе */
  box-shadow: 0 0 0 3px rgba(76, 175, 80, 0.2); /* Легкая тень */
}
```

**Объяснение стилей:**

1. **`.task-info_title`**:
   - `cursor: pointer` - курсор меняется на руку при наведении
   - `padding: 4px` - отступы для лучшего UX
   - `transition` - плавная анимация изменения цвета фона

2. **`.task-info_title:hover`**:
   - Легкий серый фон при наведении (визуальная подсказка о кликабельности)

3. **`.task-info_title-input`**:
   - `width: 100%` - input на всю ширину контейнера
   - `font-family: inherit` - шрифт как у родителя (визуальная согласованность)
   - `border: 2px solid #4CAF50` - зеленая рамка (показывает режим редактирования)

4. **`.task-info_title-input:focus`**:
   - Более темная рамка и тень при фокусе

---

## Шаг 8: Улучшение UX (User Experience)

### 8.1. Синхронизация editingTitle с task.title

**Проблема:** Если задача обновится извне (например, через чекбокс), `editingTitle` не обновится.

**Решение:** Используем `useEffect` для синхронизации

**Где:** После объявления состояний в `Task.tsx`

```typescript
import { useState, useEffect } from "react";

function Task({ task, onToggle, onUpdateTitle }: Readonly<TaskProps>) {
  const [isEditing, setIsEditing] = useState(false);
  const [editingTitle, setEditingTitle] = useState(task.title);

  // Синхронизируем editingTitle с task.title
  useEffect(() => {
    setEditingTitle(task.title);
  }, [task.title]);

  // ... остальные функции
}
```

**Что такое useEffect?**

`useEffect` - это хук React для побочных эффектов (side effects). Он выполняет код после рендера компонента.

**Синтаксис:**
```typescript
useEffect(() => {
  // Код, который нужно выполнить
}, [dependency1, dependency2]); // Массив зависимостей
```

**Когда выполняется код в useEffect?**
1. После первого рендера компонента
2. Каждый раз, когда изменяются зависимости из массива

В нашем случае:
- Когда `task.title` изменится
- `setEditingTitle(task.title)` обновит локальное состояние

### 8.2. Обновление импорта:

**Где:** В строке 1

**Было:**
```typescript
import { useState } from "react";
```

**Стало:**
```typescript
import { useState, useEffect } from "react";
```

---

## Шаг 9: Тестирование

### Проверьте функционал:

1. **Запустите dev сервер:**
   ```bash
   npm run dev
   ```

2. **Откройте приложение в браузере**

3. **Протестируйте базовый функционал:**
   - [ ] При клике на title появляется input
   - [ ] Input автоматически получает фокус
   - [ ] В input отображается текущий title
   - [ ] Можно вводить текст в input
   - [ ] При нажатии Enter изменения сохраняются
   - [ ] При клике вне input (blur) изменения сохраняются
   - [ ] При нажатии Escape редактирование отменяется

4. **Протестируйте граничные случаи:**
   - [ ] Попробуйте ввести пустую строку → title не должен измениться
   - [ ] Попробуйте ввести только пробелы → title не должен измениться
   - [ ] Измените title и переключите чекбокс → задача переместится с новым title
   - [ ] Начните редактировать и нажмите Escape → title вернется к исходному

5. **Проверьте стили:**
   - [ ] При наведении на title появляется подсветка
   - [ ] Курсор меняется на pointer (руку) при наведении на title
   - [ ] Input имеет зеленую рамку
   - [ ] При фокусе на input появляется тень

6. **Запустите линтер:**
   ```bash
   npm run lint
   ```

7. **Проверьте типы TypeScript:**
   ```bash
   npx tsc --noEmit
   ```

---

## Дополнительные концепции React для понимания

### 1. Контролируемые vs Неконтролируемые компоненты

**Неконтролируемый input:**
```typescript
<input type="text" defaultValue="Hello" />
```
- React не управляет значением
- DOM хранит значение
- Сложнее валидировать и управлять

**Контролируемый input:**
```typescript
const [value, setValue] = useState("Hello");
<input
  type="text"
  value={value}
  onChange={(e) => setValue(e.target.value)}
/>
```
- React управляет значением через state
- Единый источник правды (single source of truth)
- Легче валидировать и управлять

### 2. Условный рендеринг (Conditional Rendering)

В React есть несколько способов условно отображать элементы:

**Тернарный оператор (наш случай):**
```typescript
{condition ? <ComponentA /> : <ComponentB />}
```

**Логическое И (&&):**
```typescript
{condition && <Component />}
```

**Переменная:**
```typescript
let content;
if (condition) {
  content = <ComponentA />;
} else {
  content = <ComponentB />;
}
return <div>{content}</div>;
```

### 3. События в React

React оборачивает нативные события браузера в **SyntheticEvent** для кросс-браузерной совместимости.

**Типы событий:**
- `onClick` - клик мыши
- `onChange` - изменение значения (для input, select, textarea)
- `onBlur` - потеря фокуса
- `onFocus` - получение фокуса
- `onKeyDown` - нажатие клавиши
- `onKeyUp` - отпускание клавиши
- `onSubmit` - отправка формы

**Обработка события:**
```typescript
const handleClick = (e: React.MouseEvent<HTMLButtonElement>) => {
  e.preventDefault(); // Отменить действие по умолчанию
  e.stopPropagation(); // Остановить всплытие события
  // Ваш код
};
```

### 4. Хук useEffect и жизненный цикл

`useEffect` заменяет методы жизненного цикла классовых компонентов.

**Выполнить один раз при монтировании:**
```typescript
useEffect(() => {
  console.log("Компонент примонтирован");
}, []); // Пустой массив зависимостей
```

**Выполнить при изменении переменной:**
```typescript
useEffect(() => {
  console.log("count изменился:", count);
}, [count]); // count в зависимостях
```

**Выполнить при каждом рендере:**
```typescript
useEffect(() => {
  console.log("Компонент отрендерился");
}); // Нет массива зависимостей (не рекомендуется)
```

**С очисткой (cleanup):**
```typescript
useEffect(() => {
  const timer = setInterval(() => {
    console.log("tick");
  }, 1000);

  // Cleanup функция
  return () => {
    clearInterval(timer);
  };
}, []);
```

### 5. Хук useCallback vs useMemo

**useCallback** - мемоизирует функцию:
```typescript
const memoizedCallback = useCallback(
  () => {
    doSomething(a, b);
  },
  [a, b]
);
```

**useMemo** - мемоизирует значение (результат вычисления):
```typescript
const memoizedValue = useMemo(
  () => computeExpensiveValue(a, b),
  [a, b]
);
```

**Когда использовать?**
- `useCallback` - для функций, передаваемых в props (оптимизация)
- `useMemo` - для дорогих вычислений (оптимизация)
- В большинстве случаев оптимизация не нужна (не преждевременная оптимизация!)

---

## Возможные ошибки и их решения

### Ошибка 1: Input не появляется при клике на title

**Причина:** Не добавлен `onClick` на h3 или функция `handleStartEditing` не вызывается

**Решение:** Убедитесь, что:
```typescript
<h3 onClick={handleStartEditing}>{task.title}</h3>
```

### Ошибка 2: Input не обновляется при вводе текста

**Причина:** Не добавлен `onChange` или неправильно обновляется state

**Решение:** Убедитесь, что input контролируемый:
```typescript
<input
  value={editingTitle}
  onChange={(e) => setEditingTitle(e.target.value)}
/>
```

### Ошибка 3: Изменения не сохраняются

**Причина:**
- Не вызывается `onUpdateTitle`
- Функция `updateTaskTitle` не передана через props

**Решение:** Проверьте цепочку передачи функции:
1. `TasksFetch` → создает `updateTaskTitle`
2. `TasksFetch` → передает в `TaskList` как `onUpdateTitle`
3. `TaskList` → передает в `Task` как `onUpdateTitle`
4. `Task` → вызывает в `handleSaveTitle`

### Ошибка 4: TypeScript ругается на типы событий

**Причина:** Неправильный тип для обработчика события

**Решение:** Используйте правильные типы:
```typescript
// Клавиатура
(e: React.KeyboardEvent<HTMLInputElement>) => {}

// Мышь
(e: React.MouseEvent<HTMLHeadingElement>) => {}

// Форма
(e: React.FormEvent<HTMLFormElement>) => {}

// Изменение input
(e: React.ChangeEvent<HTMLInputElement>) => {}
```

### Ошибка 5: autoFocus не работает

**Причина:** Может быть проблема с фокусом в браузере

**Решение:** Используйте ref для принудительной фокусировки:
```typescript
import { useState, useEffect, useRef } from "react";

function Task({ task, onToggle, onUpdateTitle }: Readonly<TaskProps>) {
  const [isEditing, setIsEditing] = useState(false);
  const [editingTitle, setEditingTitle] = useState(task.title);
  const inputRef = useRef<HTMLInputElement>(null);

  useEffect(() => {
    if (isEditing && inputRef.current) {
      inputRef.current.focus();
      inputRef.current.select(); // Выделяет весь текст
    }
  }, [isEditing]);

  // ... остальной код

  return (
    // ...
    <input
      ref={inputRef}
      // ... остальные props
    />
  );
}
```

### Ошибка 6: При Escape изменения все равно сохраняются

**Причина:** `onBlur` срабатывает раньше, чем `onKeyDown` обрабатывает Escape

**Решение:** Добавьте флаг для отслеживания отмены:
```typescript
const [isEditing, setIsEditing] = useState(false);
const [editingTitle, setEditingTitle] = useState(task.title);
const isCancelled = useRef(false);

const handleSaveTitle = () => {
  if (isCancelled.current) {
    isCancelled.current = false;
    return;
  }
  onUpdateTitle(task.id, editingTitle);
  setIsEditing(false);
};

const handleCancelEditing = () => {
  isCancelled.current = true;
  setEditingTitle(task.title);
  setIsEditing(false);
};

const handleKeyDown = (e: React.KeyboardEvent<HTMLInputElement>) => {
  if (e.key === "Enter") {
    handleSaveTitle();
  } else if (e.key === "Escape") {
    handleCancelEditing();
  }
};
```

---

## Дополнительные улучшения (необязательно)

После выполнения основной задачи можете попробовать:

### 1. Валидация длины title
```typescript
const updateTaskTitle = useCallback(
  (taskId: number, newTitle: string) => {
    const trimmedTitle = newTitle.trim();

    // Проверяем длину
    if (trimmedTitle === "" || trimmedTitle.length > 200) {
      return;
    }

    setTodos(todos.map(todo =>
      todo.id === taskId
        ? { ...todo, title: trimmedTitle }
        : todo
    ));
  },
  [todos]
);
```

### 2. Показать индикатор ошибки
```typescript
const [error, setError] = useState<string | null>(null);

const handleSaveTitle = () => {
  if (editingTitle.trim() === "") {
    setError("Название не может быть пустым");
    return;
  }
  if (editingTitle.length > 200) {
    setError("Название слишком длинное (макс. 200 символов)");
    return;
  }

  setError(null);
  onUpdateTitle(task.id, editingTitle);
  setIsEditing(false);
};

// В JSX:
{error && <p className={classes["task-error"]}>{error}</p>}
```

### 3. Добавить кнопки "Сохранить" и "Отмена"
```typescript
{isEditing && (
  <div className={classes["task-edit-actions"]}>
    <button onClick={handleSaveTitle}>Сохранить</button>
    <button onClick={handleCancelEditing}>Отмена</button>
  </div>
)}
```

### 4. Анимация появления input
```css
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: scale(0.95);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

.task-info_title-input {
  animation: fadeIn 0.2s ease-out;
}
```

### 5. Сохранение в localStorage
```typescript
useEffect(() => {
  localStorage.setItem('todos', JSON.stringify(todos));
}, [todos]);

// При загрузке:
const [todos, setTodos] = useState<Todo[]>(() => {
  const saved = localStorage.getItem('todos');
  return saved ? JSON.parse(saved) : [];
});
```

### 6. Двойной клик вместо одинарного
```typescript
<h3 onDoubleClick={handleStartEditing}>{task.title}</h3>
```

---

## Полезные ресурсы

- [React Hooks Reference](https://react.dev/reference/react)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/handbook/intro.html)
- [React Events](https://react.dev/learn/responding-to-events)
- [Controlled Components](https://react.dev/reference/react-dom/components/input#controlling-an-input-with-a-state-variable)
- [useEffect Guide](https://react.dev/reference/react/useEffect)

---

## Чек-лист выполнения

**Компонент Task:**
- [ ] Импортирован `useState` и `useEffect`
- [ ] Добавлено состояние `isEditing`
- [ ] Добавлено состояние `editingTitle`
- [ ] Создана функция `handleStartEditing`
- [ ] Создана функция `handleSaveTitle`
- [ ] Создана функция `handleCancelEditing`
- [ ] Создана функция `handleKeyDown`
- [ ] Добавлен `useEffect` для синхронизации `editingTitle`
- [ ] Обновлен интерфейс `TaskProps`
- [ ] Добавлен параметр `onUpdateTitle`
- [ ] Реализован условный рендеринг (input или h3)
- [ ] Добавлен `onClick` на h3
- [ ] Настроен контролируемый input

**Компонент TaskList:**
- [ ] Обновлен интерфейс `TaskListProps`
- [ ] Добавлен параметр `onUpdateTitle`
- [ ] Функция передана в компонент `Task`

**Компонент TasksFetch:**
- [ ] Создана функция `updateTaskTitle` с `useCallback`
- [ ] Функция передана в оба компонента `TaskList`

**Стили (Task.module.css):**
- [ ] Добавлены стили для `.task-info_title`
- [ ] Добавлены стили для `.task-info_title:hover`
- [ ] Добавлены стили для `.task-info_title-input`
- [ ] Добавлены стили для `.task-info_title-input:focus`

**Тестирование:**
- [ ] Приложение работает корректно
- [ ] Input появляется при клике на title
- [ ] Изменения сохраняются при Enter и blur
- [ ] Escape отменяет редактирование
- [ ] Пустые строки не сохраняются
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в линтере (`npm run lint`)
- [ ] Нет ошибок TypeScript (`npx tsc --noEmit`)

Удачи! 🚀
