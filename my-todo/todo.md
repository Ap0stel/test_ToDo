# TODO: Добавить возможность перемещения задач между Active и Completed

## Общая концепция

Нужно добавить функционал, который позволит пользователю отмечать задачи как выполненные (и наоборот) с помощью чекбокса. При изменении статуса задача должна автоматически перемещаться между списками "Активные" и "Завершенные".

---

## Шаг 1: Понимание текущей архитектуры

### Структура компонентов:
```
App
  └── TasksFetch (управляет состоянием и загрузкой данных)
      └── TaskList (отображает список задач)
          └── Task (отображает одну задачу с чекбоксом)
```

### Текущее состояние данных:

В компоненте `TasksFetch.tsx` находится:
- **`todos`** - массив всех задач (State)
- **`activeTasks`** - вычисляемое значение через `useMemo` (задачи с `completed: false`)
- **`completedTasks`** - вычисляемое значение через `useMemo` (задачи с `completed: true`)

---

## Шаг 2: Создание функции для изменения статуса задачи

### Что такое State и как с ним работать?

**State** - это данные компонента, которые могут изменяться. В React используется хук `useState`:

```typescript
const [todos, setTodos] = useState<Todo[]>([]);
//     ↑        ↑
//  значение  функция для изменения
```

**ВАЖНО:** В React нельзя напрямую изменять state! Нужно создавать новую копию
❌ **Неправильно:**
```typescript
todos[0].completed = true;  // Мутация state - так делать нельзя!
```

✅ **Правильно:**
```typescript
setTodos([...todos, newTodo]);  // Создаем новый массив
```

### Добавьте функцию в TasksFetch.tsx:

**Где:** После объявления состояния `todos`, примерно на строке 10.

```typescript
const toggleTaskComplete = (taskId: number) => {
    // Создаем НОВЫЙ массив, где изменяем нужную задачу
    setTodos(todos.map(todo =>
        // Если это нужная задача - меняем completed
        todo.id === taskId
            ? { ...todo, completed: !todo.completed }  // Создаем новый объект с изменённым свойством
            : todo  // Оставляем остальные задачи без изменений
    ));
};
```

**Объяснение построчно:**

1. `todos.map()` - проходим по всем задачам и создаем НОВЫЙ массив
2. `todo.id === taskId` - проверяем, это та задача, которую нужно изменить?
3. `{ ...todo, completed: !todo.completed }` - используем spread оператор `...` чтобы скопировать все свойства задачи, но перезаписываем `completed` на противоположное значение
4. `!todo.completed` - это инверсия булева значения (true → false, false → true)

---

## Шаг 3: Передача функции вниз по дереву компонентов

### Концепция: Props Drilling

В React данные передаются от родителя к ребенку через **props**. Чтобы функция из `TasksFetch` попала в `Task`, нужно пройти через `TaskList`.
EOF
### 3.1. Передайте функцию в TaskList (TasksFetch.tsx):

**Где:** В строках 38-39, где рендерятся компоненты `<TaskList>`

**Было:**
```typescript
<TaskList title='Активные' tasks={activeTasks} />
<TaskList title='Завершенные' tasks={completedTasks} />
```

**Стало:**
```typescript
<TaskList
    title='Активные'
    tasks={activeTasks}
    onToggleComplete={toggleTaskComplete}  // Добавили новый prop
/>
<TaskList
    title='Завершенные'
    tasks={completedTasks}
    onToggleComplete={toggleTaskComplete}  // Добавили новый prop
/>
```

### 3.2. Обновите типы в TaskList.tsx:

**Где:** В интерфейсе `TaskListProps` (строка 5)

**Было:**
```typescript
interface TaskListProps {
    tasks: Todo[];
    title: string;
}
```

**Стало:**
```typescript
interface TaskListProps {
    tasks: Todo[];
    title: string;
    onToggleComplete: (taskId: number) => void;  // Добавили тип для функции
}
```

**Что это значит?**
- `(taskId: number)` - функция принимает один параметр типа number
- `=> void` - функция ничего не возвращает (void = пустота)

### 3.3. Получите функцию в параметрах TaskList:

**Где:** В строке 10, в деструктуризации параметров

**Было:**
```typescript
export default function TaskList({ tasks, title }: TaskListProps) {
```

**Стало:**
```typescript
export default function TaskList({ tasks, title, onToggleComplete }: TaskListProps) {
```

### 3.4. Передайте функцию в Task:

**Где:** В строке 18-19, где рендерится компонент `<Task>`

**Было:**
```typescript
tasks.map((task) => {
    return <Task task={task} key={task.id} />
})
```

**Стало:**
```typescript
tasks.map((task) => {
    return <Task
        task={task}
        key={task.id}
        onToggleComplete={onToggleComplete}  // Прокидываем дальше
    />
})
```

---

## Шаг 4: Используйте функцию в компоненте Task

### 4.1. Обновите типы в Task.tsx:

**Где:** В интерфейсе `TaskProps` (строка 4)

**Было:**
```typescript
interface TaskProps {
    task: Todo;
}
```

**Стало:**
```typescript
interface TaskProps {
    task: Todo;
    onToggleComplete: (taskId: number) => void;
}
```

### 4.2. Получите функцию в параметрах:

**Где:** В строке 8

**Было:**
```typescript
function Task({ task }: TaskProps) {
```

**Стало:**
```typescript
function Task({ task, onToggleComplete }: TaskProps) {
```

### 4.3. Добавьте обработчик onChange для чекбокса:

**Где:** В строке 12-16, где находится `<input type="checkbox">`

**Было:**
```typescript
<input
    type='checkbox'
    id={`checkbox-${task.title}`}
    defaultChecked={task.completed}
/>
```

**Стало:**
```typescript
<input
    type='checkbox'
    id={`checkbox-${task.id}`}
    checked={task.completed}
    onChange={() => onToggleComplete(task.id)}
/>
```

**Изменения объяснены:**

1. `id={\`checkbox-${task.id}\`}` - используем id вместо title (id уникален)
2. `defaultChecked` → `checked` - меняем на контролируемый инпут
   - **defaultChecked** = только начальное значение (React не контролирует)
   - **checked** = полностью контролируемое значение (управляется через state)
3. `onChange={() => onToggleComplete(task.id)}` - при клике вызываем функцию

**Что происходит при клике:**
1. Пользователь кликает на чекбокс
2. Срабатывает `onChange`
3. Вызывается `onToggleComplete(task.id)`
4. Функция в `TasksFetch` обновляет state `todos`
5. React перерисовывает компоненты
6. `useMemo` пересчитывает `activeTasks` и `completedTasks`
7. Задача перемещается между списками!

---

## Шаг 5: Понимание useMemo и зависимостей

### Что такое useMemo?

`useMemo` - это хук React для **мемоизации** (кэширования) вычислений. Он пересчитывает значение только когда меняются зависимости.

**Синтаксис:**
```typescript
const value = useMemo(
    () => expensiveCalculation(),  // Функция вычисления
    [dependency1, dependency2]      // Массив зависимостей
);
```

### Проверьте зависимости в TasksFetch.tsx:

**Где:** Строки 24-32

**Проблема:** В `activeTasks` пустой массив зависимостей `[]`:

```typescript
const activeTasks = useMemo(
    () => todos.filter((task) => !task.completed),
    []  // ⚠️ Проблема! Мы используем todos, но не указали в зависимостях
);
```

**Почему это проблема?**
- Внутри useMemo мы используем `todos`
- Но в зависимостях его нет
- Значит, когда `todos` изменится, `activeTasks` НЕ пересчитается!
- Задачи не будут перемещаться между списками

**Исправление:**
```typescript
const activeTasks = useMemo(
    () => todos.filter((task) => !task.completed),
    [todos]  // ✅ Указываем todos в зависимостях
);
```

**Как это работает:**
1. `todos` изменяется (через `setTodos`)
2. React видит, что зависимость `[todos]` изменилась
3. React пересчитывает `activeTasks` и `completedTasks`
4. Компоненты перерисовываются с новыми списками

---

## Шаг 6: Тестирование

### Проверьте функционал:

1. **Запустите dev сервер:**
   ```bash
   npm run dev
   ```

2. **Откройте приложение в браузере**

3. **Протестируйте:**
   - [ ] Клик на чекбокс активной задачи → задача перемещается в "Завершенные"
   - [ ] Клик на чекбокс завершенной задачи → задача перемещается в "Активные"
   - [ ] Количество задач в списках меняется корректно
   - [ ] Не возникает ошибок в консоли браузера

4. **Запустите линтер:**
   ```bash
   npm run lint
   ```
   - Не должно быть ошибок и предупреждений

5. **Проверьте типы TypeScript:**
   ```bash
   npx tsc --noEmit
   ```
   - Не должно быть ошибок типизации

---

## Дополнительные концепции React для понимания

### 1. Однонаправленный поток данных

```
TasksFetch (state: todos)
    ↓ props
TaskList (получает: tasks, onToggleComplete)
    ↓ props
Task (получает: task, onToggleComplete)
    ↓ событие onChange
обратно вверх через callback
    ↑
TasksFetch (обновляет state через setTodos)
```

**Правило:** Данные идут вниз (props), события идут вверх (callbacks).

### 2. Контролируемые компоненты

**Неконтролируемый (uncontrolled):**
```typescript
<input defaultChecked={true} />  // React НЕ управляет состоянием
```

**Контролируемый (controlled):**
```typescript
<input
    checked={isChecked}           // React управляет через state
    onChange={(e) => setIsChecked(e.target.checked)}
/>
```

В контролируемых компонентах React является "источником правды" (single source of truth).

### 3. Иммутабельность (Immutability)

React требует создавать новые копии данных вместо изменения существующих:

**Массивы:**
```typescript
// Добавить элемент
const newArray = [...oldArray, newItem];

// Изменить элемент
const newArray = oldArray.map(item =>
    item.id === targetId ? { ...item, changed: true } : item
);

// Удалить элемент
const newArray = oldArray.filter(item => item.id !== targetId);
```

**Объекты:**
```typescript
// Изменить свойство
const newObject = { ...oldObject, property: newValue };

// Добавить свойство
const newObject = { ...oldObject, newProperty: value };
```

### 4. Зачем нужна иммутабельность?

React сравнивает старый и новый state для определения, нужно ли перерисовывать компонент:

```typescript
// Мутация (плохо)
todos[0].completed = true;
setTodos(todos);  // React думает что ничего не изменилось!
                  // Ссылка на массив та же

// Новая копия (хорошо)
setTodos(todos.map(todo =>
    todo.id === 1 ? { ...todo, completed: true } : todo
));  // React видит новую ссылку → перерисовывает
```

---

## Возможные ошибки и их решения

### Ошибка 1: Задачи не перемещаются между списками

**Причина:** Не добавлена зависимость `todos` в `useMemo`

**Решение:** Добавьте `[todos]` в массив зависимостей:
```typescript
const activeTasks = useMemo(
    () => todos.filter((task) => !task.completed),
    [todos]
);
```

### Ошибка 2: TypeScript ругается на props

**Причина:** Не обновлены интерфейсы

**Решение:** Убедитесь, что добавили `onToggleComplete` во все интерфейсы:
- `TaskListProps`
- `TaskProps`

### Ошибка 3: Чекбокс не реагирует на клики

**Причина:** Используется `defaultChecked` вместо `checked`

**Решение:** Используйте контролируемый компонент:
```typescript
<input
    type='checkbox'
    checked={task.completed}
    onChange={() => onToggleComplete(task.id)}
/>
```

### Ошибка 4: ESLint предупреждает о exhaustive-deps

**Причина:** Пустой массив зависимостей в `useMemo`

**Решение:** Добавьте все используемые переменные в массив зависимостей

---

## Дополнительное задание (необязательно)

После выполнения основной задачи можете попробовать:

1. **Добавить анимацию** при перемещении задач (используйте CSS transitions)
2. **Сохранять состояние** в localStorage (используйте `useEffect`)
3. **Добавить счетчики** количества задач в каждом списке
4. **Добавить кнопку "Удалить"** для задач
5. **Добавить фильтр** "Показать все" / "Только активные" / "Только завершенные"

---

## Полезные ресурсы

- [React Hooks](https://react.dev/reference/react)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/handbook/intro.html)
- [Thinking in React](https://react.dev/learn/thinking-in-react)
- [Managing State](https://react.dev/learn/managing-state)

---

## Чек-лист выполнения

- [ ] Создана функция `toggleTaskComplete` в `TasksFetch.tsx`
- [ ] Функция передана через props в `TaskList`
- [ ] Обновлен интерфейс `TaskListProps`
- [ ] Функция передана из `TaskList` в `Task`- [ ] Обновлен интерфейс `TaskProps`
- [ ] Добавлен обработчик `onChange` на чекбокс
- [ ] Изменен `defaultChecked` на `checked`
- [ ] Исправлен массив зависимостей в `useMemo`
- [ ] Приложение работает корректно
- [ ] Нет ошибок в линтере
- [ ] Нет ошибок TypeScript

Удачи! 🚀
